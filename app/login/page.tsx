import { AuthPage } from "@/components/auth-ui";

export const metadata = { title: "Sign in | A-Glory Hair and Cosmetics" };

export default function LoginPage() {
  return <AuthPage mode="login" />;
}
